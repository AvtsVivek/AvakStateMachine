<Window x:Class="Avak.StateMachine.WpfApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Avak.StateMachine.WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" Content="{Binding CurrentPageViewModel}" />
        <Border Grid.Row="1">
            <ItemsControl x:Name="ItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <Button Content="Go to View 1" Command="{Binding ButtonClick}" CommandParameter="1" Width="90" Height="30" Margin="0,10" />
                <Button Content="Go to View 2" Command="{Binding ButtonClick}" CommandParameter="2" Width="90" Height="30" />
                <Button Content="Go to View 3" Command="{Binding ButtonClick}" CommandParameter="3" Width="90" Height="30" />
            </ItemsControl>
        </Border>
    </Grid>
</Window>
